{
  "name": "@clawdbox/telegram-webhook",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker for handling Telegram webhook updates",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "effect": "^3.19.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250805.0",
    "@effect/vitest": "^0.27.0",
    "@types/bun": "latest",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0",
    "wrangler": "^4.24.1"
  }
}
