{
  "name": "clawdbox",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "for pkg in agent-container agent-worker iac operator telegram-webhook takopi-worker; do echo \"=== $pkg ===\" && (cd packages/$pkg && bunx tsc --noEmit) || exit 1; done",
    "test": "bun run test:all",
    "test:all": "bun run test:iac && bun run test:telegram && bun run test:agent && bun run test:agent-worker && bun run test:takopi-worker",
    "test:iac": "cd packages/iac && bun test",
    "test:telegram": "cd packages/telegram-webhook && bun test",
    "test:agent": "cd packages/agent-container && bun test",
    "test:agent-worker": "cd packages/agent-worker && bun test",
    "test:takopi-worker": "cd packages/takopi-worker && bun test"
  },
  "devDependencies": {
    "pathe": "^2.0.3",
    "typescript": "^5.9.3"
  }
}
