{
  "name": "clawdbox",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "for pkg in agent-container agent-worker iac; do echo \"=== $pkg ===\" && (cd packages/$pkg && bunx tsc --noEmit) || exit 1; done",
    "test": "bun run test:all",
    "test:all": "bun run test:iac && bun run test:agent-container && bun run test:agent-worker",
    "test:iac": "cd packages/iac && bun test",
    "test:agent-container": "cd packages/agent-container && bun test",
    "test:agent-worker": "cd packages/agent-worker && bun test"
  },
  "devDependencies": {
    "pathe": "^2.0.3",
    "typescript": "^5.9.3"
  }
}
