{
  "name": "clawdbox",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "for pkg in agent-container agent-worker iac operator telegram-webhook; do echo \"=== $pkg ===\" && (cd packages/$pkg && bunx tsc --noEmit) || exit 1; done",
    "test": "cd packages/iac && bunx vitest run",
    "test:iac": "cd packages/iac && bunx vitest run"
  },
  "devDependencies": {
    "pathe": "^2.0.3",
    "typescript": "^5.9.3"
  }
}
