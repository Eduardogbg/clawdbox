read CLAUDE.md ; read docs/tasks/000_specs and 001_... ; make sure we are depending on the local forks/alchemy-effect fork. commit after every iteration. keep the TODO.md up-to-date; work on the entire project spec, one step at a time. remember to always have good integration tests especially concerning iac. test all alchemy resources we add with actually trying to deploy them via iac; try to automate iac tests; eventually i need you to start working on telegram stuff, but we won't have an API key so maybe defer that for a bit; but do try to get a claude agent sdk container running on cloudflare containers, together with whatever else we need (R2 for repo? workers for whatever?). I'll be out all night so please be organized and make steady progress. remember we didn't test our alchemy-effect fork very well but if you do need to change it follow the instructions on the tasks folder, regarding which commit to use as reference etc.
