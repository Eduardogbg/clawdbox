read CLAUDE.md, follow instructions. act on `docs/tasks/001_alchemy_effect_cloudflare`. read 101 and 102. your job is to:

1. understand itty, what's the current state why they haven't rolled it out yet
2. understand how they use the cloudflare sdk/ad-hoc fetches. since we want to contribute back decide which to use for the resources we develop.
3. write an appropriately prefixed (104) report
