read CLAUDE.md. read `docs/tasks/001_alchemy_effect_cloudflare`. create a plan for how to patch the resources in the local forks we made to better suit the alchemy-effect standards, according to what was found on 104. this means stop using the SDK for the buggy resources (i think it's just secrets store) (which would make the forks useless since we only change that but let's keep them just in case, shouldn't change much on our side). write the plan to 106