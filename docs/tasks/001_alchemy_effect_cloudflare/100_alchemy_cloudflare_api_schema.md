read CLAUDE.md; read references/*; read 001_alchemy_effect_cloudflare/*;

figure out how `alchemy` and `alchemy-effect` deal with this problem on cloudflare api schema. do they depend on it or the sdk, or do they build their own interface and types, considering the official source is often wrong?
